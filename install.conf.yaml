- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zsh: zsh
    ~/.zshrc: zshrc
    ~/.tmux.conf: tmux.conf
    ~/.gitconfig: gitconfig
    ~/.gitignore: gitignore

- shell:
  - [git submodule update --init --recursive, Updating submodules]
  - [git config user.email "piotr@zaborowski.pro", Setting git email]
  - [git config user.name "Piotr Zaborowski", Setting git username]
