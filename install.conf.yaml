- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zsh: zsh
    ~/.zshrc: zshrc
    ~/.tmux.conf: tmux.conf
    ~/.gitconfig: gitconfig
    ~/.gitignore: gitignore

- shell:
  - [git submodule update --init --recursive, Updating submodules]
